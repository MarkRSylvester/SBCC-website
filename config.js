// Configuration settings
const config = {
    airtable: {
        baseId: 'appOWFyYIGbLoKalt',
        apiKey: process.env.AIRTABLE_API_KEY || 'your-api-key-here' // Use environment variable
    }
};

export default config; 